coverage:
  status:
    project:
      default:
        target: auto
        threshold: "1%"
        if_not_found: failure

    patch:
      default:
        target: "90%"
        threshold: "0%"
        if_not_found: failure
        only_pulls: true
